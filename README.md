# backend

# composer install

# composer require laravel/sanctum

# php artisan config:publish cors

# frontend

# npm create vite@latest frontend

# npm install axios

#

# first push in git

# git push --set-upstream origin abbas_branch

# اول لیست یا استیج تغیرات

# git add .

# بعد آن کومیت میکنید تغیرات را

# git commit -m "توضیحات کومیت"

# بعد آن پوش میکنید تغیرات را

# git push

# تغیرات دیگر اعضای تیم را بگیری

# git pull origin abass_branch

# تغیرات خودی مین یعنی پروژه اصلی رابگیری

# git pull origin main
# برای ساخت برنچ برای خودت یا دیگران

# git checkout -b mehdi branch or every name do you want



<!-- این دستور برای -->